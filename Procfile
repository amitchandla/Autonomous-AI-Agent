# Procfile
# Used by Heroku, and optionally by Railway if you don't use its Dockerfile
# auto-detection. Binds to $PORT the same way the Dockerfile's CMD does.
web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
